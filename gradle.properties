# preferIPv4Stack works around Gradle failing to open its loopback connection
# when a VPN virtual adapter is present on Windows.
org.gradle.jvmargs=-Xmx3G -Djava.net.preferIPv4Stack=true
org.gradle.parallel=true

loader_version=0.16.10

mod_version=1.2.0
maven_group=net.notzyvex
archives_base_name=worldmenu
